# V6 手机性能优化与实测

已在原项目完成性能减法。相同 390×844 视口、普通 CPU 的首页静止主线程成本从 **130.3 降至 10.4 ms/s**；首屏图片传输由 **1,491,388 降至 316,922 字节**。晴天无内容时的环境 Canvas 清空和全局样式变量持续写入归零。帧率原本多数已接近 60，本版主要降低持续工作量和受限条件下的负担。

## 统一 4× CPU 条件

下表是同台机器、同一 Edge、同一视口/DPR、同脚本操作的 A/B。ms/s 越低表示主线程在每秒内占用越少，不是网络速度或页面加载时长。

| 操作 | V5 主线程 ms/s | V6 主线程 ms/s | 减少 | V5 / V6 帧率 |
| --- | ---: | ---: | ---: | ---: |
| 首页静止 | 554.7 | 47.2 | 91.5% | 60.0 / 60.0 |
| 首屏滚动 | 534.3 | 77.0 | 85.6% | 57.9 / 59.3 |
| 03 → 04 | 616.6 | 176.3 | 71.4% | 58.8 / 60.0 |
| 笔记本开合 | 666.5 | 146.6 | 78.0% | 60.0 / 60.0 |
| 相册开合 | 620.8 | 80.5 | 87.0% | 59.6 / 60.0 |
| 晴 / 云 / 雨切换 | 591.7 | 152.2 | 74.3% | 59.8 / 60.0 |
| 阴 / 雪切换 | 599.8 | 149.2 | 75.1% | 60.0 / 60.0 |
| 四季切换 | 705.0 | 135.3 | 80.8% | 57.4 / 60.0 |
| 昼夜切换 | 738.5 | 150.0 | 79.7% | 59.6 / 60.0 |

原始记录见 validation/v6-perf-before*.json 与 v6-perf-after*.json；逐尺寸比较见 validation/v6-perf-comparison.md。普通 CPU 与 4× CPU 分开比较。每次只运行一个测试浏览器，使用真实 rAF 和墙钟，没有假时钟、强制帧率或并行截图。

天气任务使用相同操作意图，但 V5 没有阴天入口，且原天气过渡约45秒；V6为1200ms，实际画面与颗粒强度并非完全等价。静止、滚动、物件操作和图片量的比较没有这个入口差异。

## 删除或减少的开销

- 删除整页窗帘、全幅 blur / backdrop-filter / 持续滤镜，以及相册的背景模糊。
- 删除每帧在页面根节点更新大量继承变量；变量只写到所属图层或控件，并缓存相同值。
- 合并为一个按需 World rAF。静止晴天不空刷 Canvas；降水按质量等级限频，草叶只偶尔响应阵风。
- 手机主要景深为天空、主景、前景，共2–3层；手机两层云、低档一层静止云。持续 CSS 动画由原先10个降至最多2个。
- 雨、雪与星点数量为桌面80、普通手机32、低档16；环境刷新上限24 / 12 / 8次每秒。移除持续萤火虫、飞鸟、流星等装饰调度。
- 手机使用长边1100px WebP；非当前季节、尚不需要的昼夜素材、窗边与真实相册图按需要加载。
- 手机笔记本采用二维压扁、位移、缩放和封面淡出；不运行3D透视。照片保留同一对象的轻量位移缩放。
- 移除猫图层与呼吸/行为更新；缩小35%、移到木屋边的实测仍不自然，依据第38条采用删除方案。

静态素材准备时会使用少量离屏 Canvas 着色与透明裁切；只在外观变化时处理，不是每帧滤镜。世界的天气、昼夜和季节逻辑在低档模式继续存在。

## 测试边界

首次加载仍有偶发长帧：390视口的普通CPU测试中，超过50ms的帧从1个变为2个；4×CPU从3个降至2个，V6最大间隔133.3ms。主流程和四季操作的持续负担已降低，但没有把首次加载描述成“零卡顿”。

测试是 Windows Edge 中的实际渲染、手机尺寸/触摸/DPR/核数模拟及受限 CPU 对照。没有使用实体 iPhone/Android，也未运行 Safari；这些结果不等同于所有手机硬件的帧率保证。未执行线上部署或核验 Umami 后台入库。
