# YI.WANG V5 改造架构

本版直接修改原项目的 HTML、CSS、JS 与素材目录。首页、最近、相册、小东西、随便写写、关于，分别是同一条镜头路径上的六个地标。没有框架、构建工具、GSAP、WebGL、路由服务或后端。

## 保留 / 删除 / 重构

| 类型 | 内容 |
| --- | --- |
| 保留 | 原首页及关于文案、五个栏目、六条原 PHOTOS 占位数据、WRITING 编辑入口、GitHub Pages 相对路径、原 Umami 网站 ID、昼夜插值引擎、原创昼夜景观、静态 404 与品牌元信息 |
| 删除 | 导航直接锚点跳转、以普通 smooth scroll 充当旅行、单独复制照片的全屏图片、笔记本 modal |
| 重构 | 一个可中断镜头控制器、原生滚动输入、共享环境状态、前景遮挡、分层文字、移动导航指示线、原照片节点搬运、窗边原位开合本、URL 历史及深链初始化 |
| 新增 | 全局季节与 45 秒天气渐变、冬季注册绘画、固定世界坐标的小猫、窗滴 / 檐滴及室内雨声衰减、图片预热与失败恢复、逐项验收证据 |

## 文件职责

- `world-time.js`：原连续本地时间模型，未改动。所有颜色、光照、日月位置、星空和窗灯来自数值插值；读取访问者设备时间，不读取地理位置。
- `camera-controller.js`：唯一镜头位置、速度和加速度所有者。五次 Bézier / Hermite 曲线从当前状态重定向，终点速度和加速度归零；接近路径边界时采用短制动段，不靠位置硬钳制造停顿。原生输入使用短跟随曲线，导航旅行时长随距离变化。
- `world-state.js`：只在启动创建一次。天空时间、四季权重、天气强度、固定星点、云相位、风、降水种子、猫状态机、镜头引用和声音开关共同持久存在。小猫留在草坡睡眠 / 轻微苏醒，导航不移动它。
- `script.js`：把导航、Logo、Back / Forward、滚轮、键盘和触摸接入同一个控制器；统一视觉帧调度、资源解码、原物件交互与已授权的本地合成环境声。
- `index.html`：语义化章节与固定世界图层。启动前读取 hash，避免先显示首页；无 JavaScript 时恢复普通可读文档。
- `styles.css`：空间构图、不同景深层强度、昼夜颜色、桌面与竖屏布局、动效简化和打印样式。
- `assets/`：本地插画、透明植物 / 窗框 / 猫、冬景、站标。所有环境均为原创虚构世界，与私人事实无关。

## 渲染与空间

| 层 | 内容 | 相机作用 |
| --- | --- | --- |
| 0 | 天空、日月、三层云、星空 | 时间与相位独立持续，天空基本固定 |
| 1 | 远山、湖、远景 | 约 0.24–0.3 倍镜头强度 |
| 2 | 草坡、路径、木屋、小猫 | 完整镜头推进，猫使用固定世界锚点 |
| 3 | 纸条、绳上照片、野餐角、笔记本 | 距离派生局部位置、尺度及清晰度 |
| 4 | 草花、树枝、窗框 / 窗帘 | 较强近景响应；旅行中有约 330–420 ms 物件遮挡 |
| 5 | 章节文字、导航和控制 | 数字先入 / 后出，标题其次，正文最后进入；单个 indicator 连续移动 |

原生页面提供六屏长的滚动轨道，各语义章节在镜头层中渲染。滚动条和触控仍由浏览器处理；程序写入滚动位置有来源标记，避免反过来触发第二套相机。接近地标且用户停止输入才进行轻吸附，后续输入立即接管。没有全局 wheel / touch preventDefault 或禁止缩放。

前景遮挡只是经过世界物件；世界底图始终存在。进入窗边时，窗框叠在同一片室外风景之前；离开到 About 后，近景、窗边物件和猫逐步退出，景深收回到开阔远景。

## 物件交互

照片先留出原槽，再将同一个 figure、按钮和图片节点放入全屏查看层。打开和关闭使用当下真实几何与倾角；屏幕旋转后先测量原槽，再从当前动画姿态返回。加载高清图仅替换同一 img 的来源，失败保留缩略图或优雅留白。

书始终在 `#notes` 的原桌面节点中，封面与纸页开合，额外镜头推进约 6.5%。离开窗边会合本；纸页可独立滚动，正文保持纯文本。简化动效下保持书的固定可读尺寸与位置，以透明度开合，不做大幅推进。

## 连续性、资源和性能

一个持续 requestAnimationFrame 入口调度镜头、UI、视差与环境；手机仅降低环境 Canvas 频率和细节，相机仍按显示帧更新。测量集中在初始布局、resize 和物件交互起点，属性写入有缓存。局部照片与翻页使用一次性 Web Animations，并可取消，不建立额外环境循环。

天空、季节、天气和相机使用各自的过渡记录。天气 45 秒、季节 12 秒，导航约 0.7–1.8 秒，互不覆盖。图片提前预热并 decode；主色背景和固定尺寸避免白屏、破图与布局跳动。冬季资源齐备后再开始手动季节变化。

后台停止主循环和声音，返回后继续同一个世界，并重新读取真实时间。系统减少动态或手动关闭动效时，停止持续镜头视差和粒子，导航使用约 260 ms 的当前可见权重过渡；连续改道沿当前混合状态继续，不突然显示中途栏目。

`YI_WORLD.getState()` 只提供本地只读快照用于验收，不向 Umami 上报这些状态。统计只保留八类已要求事件与受限枚举 / 序号。真实设备、线上部署和 Umami 后台写入不在本地验收范围内。
