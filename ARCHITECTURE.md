# YI.WANG 动画世界改造架构

这次直接重构原项目的 index.html、styles.css、script.js，沿用 assets 和 GitHub Pages 根目录发布结构。需求开头称 V4，末尾称 V5 改造架构；本交付统一命名 V4 动画世界，执行的是整份需求。

## 保留、删除、重构

| 保留 | 删除 / 替换 | 重构 |
| --- | --- | --- |
| YI.WANG、汪意、网页由文杰维护 | 首页白色标题卡 | 标题直接置于天空 |
| 最近、相册、小东西、随便写写、关于 | 六段独立底色、硬切屏幕 | 同一固定世界中的连续镜头 |
| PHOTOS 的 src/full/caption/date/place 数据入口 | 大号方括号占位符、人物介绍卡 | 空白照片、小纸条、物件留位、可打开笔记本 |
| 原生 dialog、键盘/触摸、焦点返回 | 旧的可选背景视频和音乐控件 | 分层场景、程序化环境声音、默认静音 |
| Umami 地址与既有网站 ID | 重复或含私人数据的统计 | 只记录功能事件及枚举状态 |
| 原生静态部署、相对资源路径 | 构建和后端依赖 | 本地运行的独立时间引擎 |

## 结构

- `world-time.js`：纯函数连续时间模型。读取访问者设备的 `new Date()`，用跨午夜关键帧平滑插值颜色、太阳/月亮位置、云亮度、草地曝光、星空、窗灯和色温。时间标签只用于说明，画面不依赖五个静态主题切换。
- `script.js`：环境调度、滚动镜头、轻视差、稀疏自然事件、Web Audio、相册与笔记本、渐进增强和 Umami 事件。后台停止渲染与声音；减少动态效果时保留实时环境状态和可读内容。
- `styles.css`：一套世界分层布局、天空中的文字导航、场景内的纸张/照片/书本交互。手机重新安排构图、横向相册和单页笔记本。
- `assets/`：原创绘制的昼夜地形、云、植物与木屋窗边素材。仅为虚构环境，不代表任何真实地点或汪意的生活。
- `index.html`：语义化章节、固定场景容器和可访问的原生对话框；无 JavaScript 时仍可阅读。
- `404.html`：同品牌的静态边缘页。所有网页资源使用相对路径；404 的返回入口兼容网站根和仓库路径。

## 镜头与交互

滚动仍采用浏览器原生滚动。缓存章节位置，滚动事件只更新目标值；动画帧以 transform 改变远景、中景、近景。章节之间用近景植物经过镜头形成遮挡。进入窗边时，透明窗景与同一户外世界叠合。相册从点击位置展开并返回；笔记本采用纸页翻转。

## 性能策略

主图压缩为本地 WebP；星星、少量萤火虫和偶发花瓣共用一个低分辨率 Canvas。手机减少图层、粒子与视差，不请求陀螺仪权限。主循环在页面不可见和静态阅读模式下停止。真实时间的插值与浏览器性能测量分别验证，不将桌面模拟结果当作所有设备的 60fps 保证。
