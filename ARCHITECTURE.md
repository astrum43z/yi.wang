# YI.WANG V6 架构

V6 保留同一个静态项目和六个地标。目标是先减少手机持续工作量，再让天气和季节变化清楚可见。无框架、构建工具、GSAP、ScrollTrigger、WebGL、后端或路由服务。

## 文件边界

| 文件 | 职责 |
| --- | --- |
| world-time.js | 连续设备本地时间、光线和日月关键帧，不读取位置 |
| camera-controller.js | 唯一镜头坐标与旅程；原生输入、导航和改道共享控制器 |
| world-state.js | 五天气、四季、质量选择、1200ms 过渡与 deriveAppearance |
| scene-painter.js | 按外观请求加载 / 解码素材，一次性着色和透明裁切，双缓冲交叉淡入 |
| script.js | 最新选择提交、按需 World rAF、局部样式写入、原照片与书本、音频和统计 |
| styles.css | 固定场景布局、画布空间、质量降级、手机二维阅读、原生对话框与减少动态 |
| index.html | 语义化地标、原内容编辑入口、固定世界画布与原生交互 |
| assets/ 与 assets/mobile/ | 桌面原图与长边 1100px 的手机 WebP |

## 从选择到可见状态

设置入口合并 season / weather / time 意图，并为请求编号。ScenePainter 先准备对应素材；只有最新请求可提交。失败保留当前已显示的世界并允许重试，过期结果不回写新状态。

world-state.js 从现有插值位置开始新的天气 / 季节过渡。地景缓冲、天空、云、光照、窗框和环境强度在同一次提交后用同一个 1200ms 时钟更新；画布层没有额外 CSS transition。快速连选的目标是从当前可见状态继续，而不是排队播放所有中间选择。是否满足所有打断边界，以 VALIDATION.md 的 V6 实测为准。

天气为 clear、cloudy、overcast、rain、snow。统一 deriveAppearance 产生天空、云色、光照、雾、湿润、降水和花量参数。手动天气不被自动天气计时器覆盖。季节支持 spring、summer、autumn、winter，以及按本地月份选择的 auto。

## 画布与景深

| 层 | 内容与更新方式 |
| --- | --- |
| Sky | 渐变天空、日月、已预着色云；天空参数局部更新 |
| Far | 仅 high 的远景画布；较轻的相机响应 |
| Main | 同一构图的地景画布，保留 1672:941 坐标，手机使用 1100px 源 |
| Foreground | 按视口预裁切的一组透明植物画布；秋仅留枝条，冬清空旧花草 |
| Window | 接近窗边才准备的透明窗框；手机保留左侧杯子比例，右侧只绘制帘子轮廓 |
| Objects / UI | 纸条、原照片、野餐角、原位书本、标题与导航 |
| Environment | 有内容时才绘制的星点、雨、雪及少量花瓣；雨天窗边加稀疏滴水 |

每个预绘制组用两张画布承接新旧外观。季节原图、昼夜混合、天空透明边界、雾、湿润色及前景着色只在外观准备时绘制。旅行不重新绘制这些图；切换期间只更新缓冲透明度。少量 Canvas 2D 着色滤镜只用于一次性预绘制，页面没有持续全幅 CSS 滤镜。

春、夏、秋、冬的昼夜地景均有对应本地图。snow 的有效地表强制使用 winter 绘画，即使当前季节选择是夏天；屋顶、栏杆与草坡积雪来自实际像素内容。旧冬前景 RGB 棋盘素材不再叠加或用 CSS 遮罩补救。

手机保留天空、主景、前景这 2–3 个主要景深平面，进入 notes 后前景让位给窗框。high 有三层云，只有两层做慢速 CSS 位移；medium 两层云，low 一层静止云。双缓冲数量不等于持续视差层数量。

## 单一按需调度

World rAF 只在相机、书本、外观过渡、需要处理的输入或局部遮挡仍活动时连续运行。相机静止时跳过章节和导航的重复更新。降水通过同一入口限频唤醒，而不再维持空转的全速循环；空白晴天环境画布直接返回，不执行 clearRect。

每秒低频时钟负责设备时间与环境事件；AUTO 地景约每 30 秒重新准备一次。星点不逐颗持续闪烁，植物只在阵风时触发一次运动。high / medium / low 的环境绘制上限为 24 / 12 / 8次每秒，粒子比例为 1 / 0.4 / 0.2；低档画布像素比进一步降低。

动态变量下放到 world、sky、cloud、header、nav、notes 或 desk-book 等所属节点，避免每帧在 documentElement 上更新大量继承属性。属性写入有缓存。scroll 只保存输入并请求一帧，处理集中到调度器；resize 防抖，几何测量集中于布局和照片开合边界。wheel / touchstart / scroll / resize 使用 passive 监听。

页面隐藏时取消 rAF 与唤醒定时器并暂停声音。恢复后读取真实时间并继续当前世界。系统减少动态与手动暂停仍允许设置和阅读；low 本身不禁用核心导航、昼夜或天气。

## 03 到 04 与物件

整页 camera-wipe / wipe-curtain 已移除。high 桌面仅在接近或离开窗边时使用约 220ms 的局部右上树叶，其容器为 45vw × 50svh，不使用整屏遮罩。手机禁用该元素，使用约 600–650ms 相邻镜头移动、轻微尺度和透明度；较远导航按距离延长。

照片通过预留槽搬运原 figure / button / img，保持真实内容节点、返回几何和焦点。手机照片不使用模糊，保留局部平移缩放；系统减少动态时改为淡入。笔记本始终位于 notes，保留原字段和翻页接口；桌面保留阅读姿态，手机没有 perspective / rotateX / rotateY 书本变换，使用二维缩放与封面淡出。减少动态时固定可读书本位置。

## 删除和保留

删除：全屏窗帘转场、持续全幅滤镜、背景模糊、旧多组连续草叶动画、空白 Canvas 刷新、重复 V4 样式与旧笔记本对话框、猫的显示与行为动画。

猫曾缩小 35% 并移到靠木屋的边缘位置。实际昼夜 / 雨天截图仍有比例、遮挡接地及夜间色温问题，因此按用户授权删除；试验记录保存在开发工作区 work/v6-cat-trial/conclusion.md。运行时仅保留 removed 状态用于诊断兼容，不运行猫状态机、不请求猫图。

保留：首页与关于原文、六条 PHOTOS、WRITING、五个栏目、原生相册 / 书本阅读、静态相对路径部署、404、站标与固定 Umami。声音由用户主动开启的本地 Web Audio 生成，不下载音频。

## 诊断与证据

YI_WORLD.getState() 提供本地只读快照，包括世界、quality、画布准备 / 过渡、镜头、书本和绘制计数；这些诊断状态不会作为统计载荷上传。

性能验收比较冻结 V5 与当前 V6 的相同工作负载，收集真实 rAF、CDP Script / Layout / RecalcStyle / TaskDuration、Canvas 调用、环境帧、图片传输和活动动画。手机视口模拟及 4× CPU 相对受限测试不等于物理手机。当前结果和未覆盖范围由 VALIDATION.md 回填，不沿用旧版通过结论。
