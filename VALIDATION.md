# V5 验证记录

本版在原项目上实现并实际运行。浏览器为 Windows 上的 Microsoft Edge 152.0.4191.66，使用真实网页渲染；手机为浏览器尺寸与触摸模拟。功能测试中的日夜/季节/天气使用受控设备时钟加速复现；性能采样没有假时钟或强制帧率参数。未执行正式网站部署，未验证实体手机或 Safari。

## 交互验收

主流程 62 项通过，页面脚本错误和本地资源错误均为 0。七条指定路线均有逐帧相机中间位置和环境连续性证据：

| 路线 | 请求时长 |
| --- | --- |
| 首页 → 最近 | 905 ms |
| 首页 → 相册 | 1090 ms |
| 首页 → 关于 | 1645 ms |
| 最近 → 小东西 | 1090 ms |
| 相册 → 随便写写 | 1090 ms |
| 随便写写 → 关于 | 905 ms |
| 关于 → 首页 | 1350 ms |

还实际覆盖：95 ms 间隔五个导航、约 58% 进度改道、滚动半程后导航、滚轮接管、Back/Forward、移动菜单与真实触摸事件反向滑动。天空、猫、Canvas、导航指示线均是原节点；世界 ID 与固定猫坐标保持，云继续前移。

宽度 320 / 390 / 600 / 768 / 1024 / 1440 / 1920 无整页横向溢出；相册另测 844×390 → 360×640 旋转。延迟主脚本 1.6 秒直接打开 gallery/notes/about，首次可见均为目标场景；刷新保持位置。模拟 GitHub Pages /repository/ 子路径加载及跨栏目导航通过；关闭 JavaScript 时所有语义栏目仍能阅读。404 实际渲染通过。

## 照片与笔记本

独立照片测试 12 组通过。测试图片仅通过浏览器响应注入，均标注为合成测试图，没有写入网站或原 PHOTOS 数据。覆盖慢缩略/高清、full-only、缩略失败高清恢复、高清失败保留缩略、双失败留白、前后切换、开场中关闭、屏幕旋转和焦点返回。全程 figure/button/img 均为原节点。归位动画终点与实际恢复后一帧的几何最大误差 0.486 px，700 ms 后稳定；原顺序不变。

笔记本在 notes 原节点中开合，不出现 dialog；翻页、滚动离开自动合本通过。手机窗景已保持插画比例，本子位于杯子旁桌面；打开后的纸页底边贴近桌面。系统 reduce 和手动“动效关”都保持固定可读书本位置及尺寸：实测开合宽度比 1、中心位移 0 px。

减少动态模式另有 6 项独立回归：重复点击当前栏目透明度保持 1；home→about 半程改去 notes，没有无关 things 栏目闪入；首个改道帧可见权重差约 0.0001；静止后持续 rAF 停止。正常模式与系统设置运行中切换也通过。

## 环境与资源

雨以 45 秒渐变，18 秒时为中间强度。进入窗边后雨、世界与猫状态保持，窗滴/檐滴显现；只创建一个音频上下文。主动开启声音后，室外雨增益约 0.731，进入室内平滑下降至约 0.287，环境声不重新起播。

冬季六个地点均保持同一雪量与季节权重；春/夏/秋也各走过六处。原全天时间模型未改，SHA-256 为 909b997113595fddc7a5245633c6238dbd25eef30f5c41d28d93bb2bf1d0beee；保留 86,400 秒采样、2,419,200 个数值断言与 86,413 个时段断言通过的证据。新版实际浏览器另测手动昼夜与相机同时推进，以及 AUTO 跨 18:30 边界的导航。

冬景首次网络失败后恢复再试成功，资源请求从首次 3 次变为重试累计 6 次，全部图片解码；失败 Promise 不再永久缓存。成功重试会恢复原图片可见状态。主色占位和固定尺寸保留，加载失败不产生破图或新私人内容。

后台处理停止音频与持续帧，回来保持世界实例并让声音保持关闭。无定位/天气接口；模拟天气、四季和小猫属于虚构环境，不推断汪意的所在地、经历或宠物。

## 相机数值检验

当前 CameraController SHA-256：c73772fee144aec914d0935bd4ec652bd063430bfb0609209804a72d746eee1d。

通过 320,928 次采样、5,365 次重定向位置/速度/加速度连续性检查、48,540 次控制点范围检查、32,360 次曲线端点检查。两端分别在 80/85/90/95% 时反向改道，无越界钳停、重回旧起点或旧完成状态覆盖新目标。原生滚动与导航用同一个位置所有者。

## 性能实测与边界

每组约 6 秒，包含 home→gallery→notes→home。无 CPU 限速、无假时钟、无强制帧率参数；其他验收浏览器关闭后顺序采样。

| 场景 | rAF FPS | p95 帧间隔 | 大于 50 ms 帧 | 长任务 |
| --- | --- | --- | --- | --- |
| 桌面白天 1440×960 | 47.75 | 20.7 ms | 1 | 0 |
| 桌面夜间 1440×960 | 48.58 | 20.7 ms | 0 | 0 |
| 手机白天 390×844 | 48.58 | 20.7 ms | 0 | 0 |
| 手机夜间 390×844 | 48.58 | 20.7 ms | 0 | 0 |

空白页基线 47.92 FPS、p95 同为 20.7 ms，说明当前宿主约 48 Hz 的调度限制。没有把本环境写成已达到 60 FPS。另一次 3.04 秒真实导航的平均主线程任务为 3.147 ms/世界帧，其中脚本 0.260 ms、布局 0.059 ms、样式重算 1.828 ms。平均值低于 16.7 ms，但不代表最慢帧或 GPU 的保证；现代手机硬件仍需上线后实机观察。

## 静态完整性与统计

22 项独立静态检查通过：49 个本地引用存在并采用相对路径，所有内联与外置 JS 语法有效，主站与 404 各仅一次原 defer Umami 脚本，原六条 PHOTOS 与改造前逐字相同，品牌及占位内容正确，.nojekyll 存在，无框架/后端构建依赖。

保留八类自定义事件：进入最近/相册/小东西/随便写写/关于、打开照片、切换昼夜预览、开启声音。以浏览器替身检查事件 API，不污染正式统计；事件只携带允许的模式枚举或照片序号。线上 Umami 后台入库没有被冒称已验证。

## 实际渲染交付

提供六处场景、清晨/白天/黄昏/入夜/深夜、冬景/雨窗、共享照片、原位开本及手机图。转场中间截图使用暂停于采样时刻的动画时钟，避免截图耗时推进镜头；每张都是实际浏览器绘制，另记录相机位置和遮挡强度。它们不是示意图，也不用于测量帧率。

完整 39 条对照见 REQUIREMENTS.md；机器可读验收摘要见 validation/results.json。ZIP 最外层为 index.html，所有代码/资源均包含，压缩包解压内容与最终项目逐文件哈希一致。现有 CNAME、仓库设置与用户真实文件请在替换时保留。
