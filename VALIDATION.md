# V4 验证记录

## 实际浏览器功能检查

使用本机 Edge（Chromium，无头浏览器）运行真实 HTML / CSS / JavaScript。

- 标题、描述、首页三行文案、五个导航、关于文案、站标、社交预览及指定 Umami 脚本核对。
- 桌面导航依次到达五章；相册展开 / 返回、首尾边界、前后按钮、方向键、Escape、背景关闭、关闭时焦点返回通过。
- 在照片飞行动画中提前关闭仍正确清理；笔记本打开、翻页、边界与关闭通过。
- 320、390、600、768、1024、1440、1920 像素宽度逐章检查，未出现整页横向溢出。相册内部的手机横向滚动是设计行为。
- 手机菜单导航、可横向滚动相册、触摸横滑处理通过；触摸事件为浏览器合成事件，未做真机手指实测。
- 运行中开启系统减少动态效果，立即停止主帧循环、相机视差及纸页动画；手机横移归零。静态模式仍能切换时间和阅读。
- 默认未创建音频上下文；主动点击开启、关闭后挂起通过。模拟 visibilitychange 隐藏状态停止声音与帧循环，恢复后画面继续、声音保持关闭。
- 检查全部本地图片解码、资源请求和 JavaScript：未发现本地资源失败或页面脚本错误。
- 直接 file 打开、HTTP 根目录、模拟 GitHub Pages 仓库子路径均可工作。嵌套 404 在自定义域名 / GitHub 根站 / 仓库站返回正确首页。

## 时间引擎

- 独立纯函数测试逐秒覆盖全天 86,400 个样本，2,419,200 项数值有限性断言及 86,413 项阶段断言通过。
- 05:00、08:00、16:30、18:30、21:00、午夜及额外美术关键帧的颜色、光照、太阳 / 月亮位置连续。
- 17:20→18:10：太阳 y 从 46.4686% 到 54.6678%，窗灯从 0.15 到 0.502963，逐秒单调。
- 浏览器页面保持打开，通过模拟设备时钟依次验证清晨、白昼、黄昏、入夜、深夜的 AUTO 更新，无需刷新。
- 手动预览存在中间插值，快速连续切换后可正确回到当前本地 AUTO；重新加载恢复 AUTO。
- 加速浏览器时钟并固定随机种子，77 秒内得到 2 次阵风、1 次夜间流星；这仅为调度测试，生产仍使用真实随机数和规定的时间间隔。
- 减少视觉动效后，明确开启声音，稀疏鸟声 / 虫鸣调度仍执行。

## Umami

URL、defer 与 ID `96161282-32cd-4d9a-9470-663d7aa265c7` 保持完整。测试替身收到了所有八类规定事件；附加数据仅为预览模式枚举或照片序号。统计被屏蔽时页面仍可操作。未向生产统计发送自动测试事件，未检验 Umami 线上后台入库。

## 最终版本性能采样

关闭其他验收浏览器后，在本机 Edge 采样；桌面 1440×960，手机模拟 390×844。

| 场景 | 样本时长 | 环境绘制帧数 | rAF 间隔中位数 | rAF 间隔 P95 | >50 ms 长任务数 |
| --- | --- | --- | --- | --- | --- |
| 桌面夜景 | 5006 ms | 301 | 16.7 ms | 16.8 ms | 0 |
| 桌面连续滚动 | 5002 ms | 301 | 16.7 ms | 16.8 ms | 0 |
| 手机尺寸夜景 | 5015 ms | 151 | 16.7 ms | 16.9 ms | 0 |

桌面以约 60 fps 为目标，手机环境 Canvas 主动限制到约 30 fps，CSS 云与草的 transform 动画由浏览器调度。这里只证明这台电脑、这些尺寸与短时样本的结果，不能作为所有手机和浏览器都达到 60 fps 的保证。没有真实 iOS / Android 或 Safari / Firefox 实测，也没有线上域名部署验收。

## 预览与交付

预览来自同一份交付源文件的真实浏览器截图，没有把设计稿冒充网站渲染。五时段截图使用模拟设备本地时钟 06:30 / 10:00 / 17:20 / 19:30 / 23:00；时间只服务于验收，不作为个人经历或日期内容。总览只是将原始截图等比例排在一起。所有真人照片及私人内容仍留白。

ZIP 以 index.html 为根入口；已校验压缩内容逐文件与已渲染目录一致。保留现有 CNAME 后可沿用 GitHub Pages 发布流程，本次未发布到线上。
